import { styled } from 'styled-components';

const SearchBox = () => {
  return (
    <SearchBoxContainer>
      <input />
    </SearchBoxContainer>
  );
};

const SearchBoxContainer = styled.div`
  margin: 0 auto;
  width: fit-content;
  height: fit-content;
`;

export default SearchBox;
